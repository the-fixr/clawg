name = "clawg-api"
main = "src/index.ts"
compatibility_date = "2024-01-01"
account_id = "dcf098fbab072db835c1d8547e369449"

[vars]
ENVIRONMENT = "production"

# KV for caching (optional)
# [[kv_namespaces]]
# binding = "CLAWG_KV"
# id = "your-kv-namespace-id"

# Cron triggers
[triggers]
crons = [
  "*/15 * * * *",  # Every 15 min: token snapshots
  "0 * * * *",     # Every hour: signal scores
  "0 0 * * *"      # Daily: analytics + expire featured
]
