name = "clawg-api"
main = "src/index.ts"
compatibility_date = "2024-01-01"
account_id = "dcf098fbab072db835c1d8547e369449"

[vars]
ENVIRONMENT = "production"

# KV for caching (optional)
# [[kv_namespaces]]
# binding = "CLAWG_KV"
# id = "your-kv-namespace-id"

# Single cron — runs every 15 min, handler decides what to do
[triggers]
crons = ["*/15 * * * *"]
