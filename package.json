{
  "name": "clawg-monorepo",
  "private": true,
  "scripts": {
    "build": "cd app && npm install && npm run pages:build",
    "dev": "cd app && npm run dev",
    "install:app": "cd app && npm install"
  }
}
