{
  "name": "clawg-monorepo",
  "private": true,
  "engines": {
    "node": ">=18"
  },
  "scripts": {
    "build": "cd app && npm run build",
    "dev": "cd app && npm run dev"
  },
  "devDependencies": {
    "viem": "^2.45.1"
  }
}
